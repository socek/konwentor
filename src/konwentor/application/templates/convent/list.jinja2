{% extends "main.jinja2" %}
{% block page_head -%}
    Konwenty {% if user.has_access_to_route('convent:add') -%}
        - <small><a href="{{route('convent:add')}}">Dodaj</a></small>
    {%- endif %}
{%- endblock %}

{% macro header() -%}
    <tr>
        <th>Nazwa</th>
        <th>Akcje</th>
    </tr>
{%- endmacro %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                    <div class="table-responsive">
                        <table id="games_on_convent_table" class="table table-striped table-bordered table-hover">
                            <thead>
                                {{ header() }}
                            </thead>
                            <tfoot>
                                {{ header() }}
                            </tfoot>

                            <tbody>
                                {%- for convent in convents %}
                                    <tr{%- if choosed_id == convent.id %} class="danger" {% endif -%}>
                                        <td>
                                            {{convent.name}}
                                        </td>
                                        <td>
                                            {% if user.has_access_to_route('convent:choose') -%}
                                                <a href="{{route('convent:choose', obj_id=convent.id)}}" class="btn btn-xs btn-success">Wybierz</a>
                                            {%- endif %}</li>

                                            {% if user.has_access_to_route('convent:delete') -%}
                                                <a href="{{route('convent:delete', obj_id=convent.id)}}" class="btn btn-xs btn-danger">USUŃ</a>
                                            {%- endif %}</li>
                                        </td>
                                {%- endfor %}
                            </tbody>
                        </table>
                    </div>
            </div>
        </div>
    </div>
{% endblock %}
