<div class="form-group">
    <label>{{label}}:</label>
    {% include '_error.jinja2' %}

    <input
        class="form-control"
        value="{{value}}"
        name="{{name}}"
        id="{{id}}"
        {%- if disabled %} disabled=disabled {% endif -%}
        >
    <button class="btn btn-warning addroom" {%- if disabled %} disabled=disabled {% endif -%} for="{{id}}">
        Dodaj pokój
    </button>
</div>
